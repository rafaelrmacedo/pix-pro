# Auth Service

Authentication microservice base for PixPro.

## Responsibilities

- User registration/login entry points
- Token issuance (mock in Sprint 1)
- Future integration with identity and RBAC

## Main endpoints

- `GET /health`
- `POST /auth/register`
- `POST /auth/login`

